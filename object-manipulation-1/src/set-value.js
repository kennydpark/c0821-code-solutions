/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
  return object;
}

// input: object, key, value
// outputL: object with value found at key property
// assign value as object at key property
// return the object
