/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// input: string (containing a word)
// output: string (containing last character of the word)
// get last index of string and return string at that index
