/* exported last */
function last(array) {
  return array[array.length - 1];
}

// input: array
// output: last element of the array
// get the last element of the array by subtracting the length by 1, and return the array at that index
